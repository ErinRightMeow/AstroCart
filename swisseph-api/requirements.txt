fastapi
uvicorn[standard]
pandas
pytz
timezonefinder
# The 'swisseph' library is a dependency for the calculation engine.
# If it's not on PyPI, ensure it is installed in your environment.
# For example: pip install path/to/swisseph-x.y.z.whl